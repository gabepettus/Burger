<h1>Burgers!</h1>

<h2>Burgers that have not been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless isDevoured}}
      {{> burgers/burger-block isDevoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if isDevouredy}}
      {{> burgers/burger-block isDevoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devour?</label><br>
    <input type="radio" name="isDevoured" value="1" checked> Devour!<br>
    <input type="radio" name="isDevoured" value="0"> Awake
  </div>

  <button type="submit">Add Burger</button>
</form>